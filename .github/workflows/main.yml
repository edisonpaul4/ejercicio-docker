on:
  push:
    branches:
    - main

jobs:
  my_job:
    name: deploy to dockerhub
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Docker Login
        uses: docker/login-action@v3.6.0
        with:
          username: edisonpaul4
          password: dckr_pat_7EW5Xcdur7TSgsPXUZONhmYD1qg
          
      - name: Construye Imagen
        run: |
          echo "Building Docker image..."
          docker build -t edisonpaul4/hola-mundo:6 .
      - name: Pushea Imagen
        run: |
          echo "Building Docker image..."
          docker push edisonpaul4/hola-mundo:6
          
